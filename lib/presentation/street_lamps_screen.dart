import 'package:flutter/material.dart';

class StreetLampsScreen extends StatelessWidget {
  const StreetLampsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: ,);
  }
}
